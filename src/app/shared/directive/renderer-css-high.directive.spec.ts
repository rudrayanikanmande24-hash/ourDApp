import { RendererCssHighDirective } from './renderer-css-high.directive';

describe('RendererCssHighDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererCssHighDirective();
    expect(directive).toBeTruthy();
  });
});
