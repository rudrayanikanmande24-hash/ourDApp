import { DirictiveToLowercaseDirective } from './dirictive-to-lowercase.directive';

describe('DirictiveToLowercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new DirictiveToLowercaseDirective();
    expect(directive).toBeTruthy();
  });
});
